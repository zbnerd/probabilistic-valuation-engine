version: '3.8'

services:
  airflow-db:
    image: postgres:17-alpine
    container_name: maple-airflow-db
    restart: always
    environment:
      POSTGRES_USER: airflow
      POSTGRES_PASSWORD: ${AIRFLOW_DB_PASSWORD:-airflow}
      POSTGRES_DB: airflow
    volumes:
      - airflow_db_data:/var/lib/postgresql/data
    networks:
      - maple-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U airflow -d airflow"]
      interval: 10s
      timeout: 5s
      retries: 5

  airflow-webserver:
    image: apache/airflow:2.10.5-python3.12
    container_name: maple-airflow-webserver
    restart: unless-stopped
    networks:
      - maple-network
    ports:
      - "8180:8180"
    user: "0:0"
    environment:
      AIRFLOW__CORE__EXECUTOR: LocalExecutor
      AIRFLOW__CORE__FERNET_KEY: ${AIRFLOW_FERNET_KEY:-}
      AIRFLOW__CORE__LOAD_EXAMPLES: 'false'
      AIRFLOW__CORE__DAGS_FOLDER: /opt/airflow/dags
<<<<<<< HEAD
      AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://airflow:airflow@172.20.0.4:5432/airflow
=======
      AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://airflow:airflow@airflow-db:5432/airflow
>>>>>>> 734e88827 (build(airflow): drop network_mode:host, switch to service DNS on maple-network)
      AIRFLOW__WEBSERVER__WEB_SERVER_PORT: "8180"
      AIRFLOW__WEBSERVER__EXPOSE_CONFIG: 'false'
      AIRFLOW__SCHEDULER__DAG_DIR_LIST_INTERVAL: "30"
      KAFKA_BOOTSTRAP_SERVERS: "kafka:29092"
    volumes:
      - ./docker/airflow/dags:/opt/airflow/dags:ro
    command: webserver
    healthcheck:
      test: ["CMD", "curl", "--fail", "http://localhost:8180/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    depends_on:
      airflow-db:
        condition: service_healthy

  airflow-scheduler:
    image: apache/airflow:2.10.5-python3.12
    container_name: maple-airflow-scheduler
    restart: unless-stopped
    networks:
      - maple-network
    user: "0:0"
    environment:
      AIRFLOW__CORE__EXECUTOR: LocalExecutor
      AIRFLOW__CORE__FERNET_KEY: ${AIRFLOW_FERNET_KEY:-}
      AIRFLOW__CORE__LOAD_EXAMPLES: 'false'
      AIRFLOW__CORE__DAGS_FOLDER: /opt/airflow/dags
<<<<<<< HEAD
      AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://airflow:airflow@172.20.0.4:5432/airflow
=======
      AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://airflow:airflow@airflow-db:5432/airflow
>>>>>>> 734e88827 (build(airflow): drop network_mode:host, switch to service DNS on maple-network)
      AIRFLOW__WEBSERVER__WEB_SERVER_PORT: "8180"
      AIRFLOW__WEBSERVER__EXPOSE_CONFIG: 'false'
      AIRFLOW__SCHEDULER__DAG_DIR_LIST_INTERVAL: "30"
      KAFKA_BOOTSTRAP_SERVERS: "kafka:29092"
    volumes:
      - ./docker/airflow/dags:/opt/airflow/dags:ro
    command: scheduler
    healthcheck:
      test: ["CMD-SHELL", "airflow jobs check --job-type SchedulerJob --hostname '$$(hostname)'"]
      interval: 30s
      timeout: 10s
      retries: 3
    depends_on:
      airflow-db:
        condition: service_healthy

networks:
  maple-network:
    external: true
    name: probabilistic-valuation-engine_maple-network

volumes:
  airflow_db_data:
