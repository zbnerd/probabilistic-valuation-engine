<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- 로그 디렉토리: 환경변수 LOG_PATH 없으면 ./logs -->
    <property name="LOG_PATH" value="${LOG_PATH:-./logs}" />

    <!-- TraceAspect 전용 Rolling File -->
    <appender name="TRACE_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/trace.log</file>

        <encoder>
            <charset>UTF-8</charset>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} : %msg%n</pattern>
        </encoder>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/trace.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <maxHistory>14</maxHistory>
            <totalSizeCap>2GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- (권장) Async로 IO 부담 완화 -->
    <appender name="TRACE_ASYNC" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>8192</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <neverBlock>true</neverBlock>
        <appender-ref ref="TRACE_FILE" />
    </appender>

    <!-- TraceAspect 로그만 별도 파일로 (루트로 전파 차단) -->
    <logger name="maple.expectation.aop.aspect.TraceAspect" level="INFO" additivity="false">
        <appender-ref ref="TRACE_ASYNC" />
    </logger>

    <!-- Console Appender (requestId 포함) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <charset>UTF-8</charset>
            <pattern>%d{HH:mm:ss.SSS} [%thread] [%X{requestId:-}] %-5level %logger{36} : %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ========================================
         Loki Appender (prod 프로파일 전용)
         Issue #143: Observability Infrastructure
         ======================================== -->
    <springProfile name="prod">
        <appender name="LOKI" class="com.github.loki4j.logback.Loki4jAppender">
            <http>
                <url>${LOKI_URL:-http://localhost:3100}/loki/api/v1/push</url>
                <connectionTimeoutMs>5000</connectionTimeoutMs>
                <requestTimeoutMs>10000</requestTimeoutMs>
            </http>
            <!-- loki4j 1.4.x: JsonEncoder를 format 레벨에서 사용 -->
            <format class="com.github.loki4j.logback.JsonEncoder">
                <label>
                    <pattern>app=maple-expectation,env=${ENV:-prod},host=${HOSTNAME:-unknown}</pattern>
                </label>
            </format>
        </appender>

        <appender name="LOKI_ASYNC" class="ch.qos.logback.classic.AsyncAppender">
            <queueSize>8192</queueSize>
            <discardingThreshold>0</discardingThreshold>
            <neverBlock>true</neverBlock>
            <appender-ref ref="LOKI" />
        </appender>

        <!-- Console + Loki for prod (debugging) -->
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
            <appender-ref ref="LOKI_ASYNC" />
        </root>
    </springProfile>

    <!-- Local/Test/CI 프로파일: Console만 사용 -->
    <!-- PR #206: CI 프로파일 추가 (GitHub Actions에서 Loki 미사용) -->
    <springProfile name="local,test,default,ci,vultr">
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
        </root>
    </springProfile>

</configuration>
